// Componente simples e reutilizável
// Demonstra como criar componentes que podem ser usados em múltiplos lugares
const MyComponent = () => {
  return (
    <div>
      {/* Mensagem que demonstra a reutilização do componente */}
      <h2>Estou sendo reaproveitado em vários lugares!</h2>
    </div>
  );
};

// Exporta para ser reutilizado em outros componentes
export default MyComponent;